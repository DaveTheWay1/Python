# file is imported used import followed by the python modules name
import arithmic
print(arithmic.add(5, 8))
print(arithmic.subtract(10, 5))
print(arithmic.multiply(12, 6))
# * from this point if you were to do a python3 calculations.py
# * the bc we imorted the arithmic file, they are connected and will run 
# * like normal as if it were all one file only.